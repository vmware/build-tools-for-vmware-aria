site_name: Build Tools for Aria Docs
repo_url: https://github.com/vmware/build-tools-for-vmware-aria
docs_dir: mkdocs
# Fail if can not resolve links
strict: true
theme:
  name: material
  logo: assets/logo.svg
  features:
    - navigation.instant
    - navigation.tracking
    - navigation.tabs
    - navigation.indexes
    - toc.follow
    - search.highlight
    - search.suggest
    - search.share
    - content.code.annotate
    - content.code.copy
    - content.footnote.tooltips
  palette:
    # Palette toggle for light mode
    - scheme: default
      primary: teal
      accent: amber
      toggle:
        icon: material/weather-night
        name: Dark Mode
    # Palette toggle for dark mode
    - scheme: slate
      primary: teal
      accent: deep orange
      toggle:
        icon: material/weather-sunny
        name: Light Mode
extra:
  version:
    provider: mike
  iac:
    latest_release: 4.24.0
  general:
    bta_name: Build Tools for VMware Aria
    bta_short_name: BTVA
    installer_bundle_profile: bundle-with-installer
  products:
    vro_short_name: Orchestrator
    vro_7_full_name: vRealize Orchestrator
    vro_7_short_name: vRO
    vro_8_full_name: Aria Automation Orchestrator
    vro_9_full_name: VCF Operations Orchestrator
    vra_8_full_name: Aria Automation
    vra_8_short_name: vRA 8
    vra_cloud_short_name: vRA Cloud
    vra_9_classic_full_name: VCF Automation VM Apps Organization
    vra_9_all_apps_full_name: VCF Automation All Apps Organization
    vra_9_full_name: VCF Automation
    vra_9_short_name: VCFA
    vrops_8_full_name: Aria Operations
    vrops_9_full_name: VCF Operations
    vrops_9_short_name: VCF Ops
    vrli_8_full_name: Aria Operations for Logs
    vrli_9_full_name: VCF Operations for Logs
  archetype:
    customer_project:
      group_id: com.company.department
      artifact_id: project.type
      maven_settings_location_hint: The location of the `settings.xml` file for **MacOS** is `~/.m2` and `C:\Users\username\\.m2` for **Windows**.
      maven_profile_name: target-env

plugins:
  - meta
  - include-markdown
  - search
  - awesome-pages
  - tags
  - macros:
      on_error_fail: true
  - mike:
      canonical_version: null
      version_selector: true
      css_dir: css
      javascript_dir: js
  - kroki:
      # See plugin docs for more info: https://pypi.org/project/mkdocs-kroki-plugin/
      server_url: !ENV [KROKI_SERVER_URL, 'https://kroki.io']
      # Diagram prefix that specifies what diagrams are rendered using Kroki
      fence_prefix: kroki-
      # Enables BlockDiag (and related diagrams)
      enable_block_diag: true
      # HTTP to use; POST stores images in the build directory
      http_method: POST
      # File types to use; see https://kroki.io/#support
      file_types: 
        - svg
      # How images are included in the resulting HTML (img, object, svg)
      tag_format: svg
      # Errors are raised as plugin errors
      fail_fast: !ENV CI
      # Custom directory for caching rendered diagrams; ".cache/kroki" stores cache in project directory
      # cache_dir: .cache/kroki
markdown_extensions:
  - pymdownx.superfences
  - pymdownx.tabbed:
      alternate_style: true
  - admonition
  - pymdownx.details
  - toc:
      permalink: true
  - footnotes
# extra_css:
#   - css/version-select.css
# extra_javascript:
#   - js/version-select.js
